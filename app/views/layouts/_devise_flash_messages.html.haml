- flash.each do |key, value|
  - if black_list_alerts.exclude? value
    %div{ class: "alert-msg alert-#{ key }" }
      %a{ href:'#', 'data-dismiss': :"alert-#{ key }", class: 'close' }
        &times;
      = "#{value}"
